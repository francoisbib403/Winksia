export enum PricingModel {
  FREE = 'free',
  FREEMIUM = 'freemium',
  PAID = 'paid',
  ENTERPRISE = 'enterprise',
  API_BASED = 'api_based',
}

export enum ToolStatus {
  DRAFT = 'draft',
  PENDING = 'pending',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
  FLAGGED = 'flagged',
}
